#pragma once
#include "include.h"

void search(const HANDLE process,const int passedVal);
void next(const HANDLE process, const int passedVal);
void write(const HANDLE process, const int passedVal);